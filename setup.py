from distutils.core import setup

requirements = [
]

setup(name='illusion',
      version='0.1',
      py_modules=['illusion'],
      install_requires=requirements,
      extras_require={
      },
      scripts=[
      ],
      )
